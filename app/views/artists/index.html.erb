<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% @artists.each do |artist| %>
      <div class="col-md-3">
      <div class="thumbnail">
        <img src="<%= artist.image_url %>" alt="">
        <div class="caption">
          <h1><%= artist.name %></h1><br>
          <p  ><%= link_to "Ver más", artist_path(artist), class:"btn btn-primary btn-lg btn-block" %></p>
        </div>
      </div>
    </div>
      <% end %>
    </div>
  </div>
</div>
